import React from 'react';
import { MessagesPageContainer, Title } from './styles';

function MessagesPage(): JSX.Element {
  return (
    <MessagesPageContainer>
      <Title>Messages page</Title>
    </MessagesPageContainer>
  );
}

export default MessagesPage;
